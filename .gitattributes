# harness 感測器的 session 日誌是 append-only 的。真正在解跨分支衝突的機制是
# **分片**:日誌一分支一個檔,兩條分支沒有共同的寫入區域,所以衝突不會產生,
# 而不是產生了再解。完整理由——包含「為什麼光有 union 不夠」那段 GitHub 把
# 開著的 PR 誤標成有衝突的來龍去脈——寫在 `.claude/hooks/decision_log.py`
# 檔頭〈為什麼一分支一個檔〉,這裡不重複一份。
#
# union 在分片之後降級成**同分支**的保底:同一條分支自己 rebase 時尾端仍可能
# 撞上,那是真的 append-only 衝突,保留兩邊的行正是想要的語意。前提是每行都能
# 獨立成立(一 session 一行、鍵序穩定),由 decision_log.py 的 to_line() 保證;
# union 不會產生半行,最壞情況只是重複行,而 harness-metrics.py 以 session id 去重。
.claude/metrics/sessions/*.jsonl merge=union

# 分片前的共享單檔。已凍結(decision_log.py 不再寫它),留著是因為刪除或改名
# 都是 delete/modify,會讓當時開著的每個 PR 各撞一次衝突——正是分片要消滅的
# 症狀。union 留著純粹是保險:它不該再被任何人寫。
.claude/metrics/sessions.jsonl merge=union
