# Cloudflare Pages 路由規則。**順序即優先序**，第一條命中就停。
#
# 靜態檔存在時 Pages 一律先送檔，這裡的規則只在「沒有對應檔案」時才生效。
#
# /assets/* 的 404：hash 過的資產檔要嘛存在、要嘛就是部署缺檔——不論哪種，
# 它都不該掉進下面的 SPA 後備。2026-08-07 正式站事故正是這樣爆的：某次
# 部署少上傳了三個 chunk，後備規則把它們變成「200 + text/html」，瀏覽器
# 的 module loader 收到 HTML 直接拒絕（Strict MIME type checking），admin
# 後台整頁進不去。回 404 至少讓錯誤誠實、可診斷，也避免瀏覽器把一份 HTML
# 快取在 .js 的網址底下、修好之後還繼續壞。
/assets/* /404.html 404

# SPA 後備：其餘路徑一律交給前端路由。
/* /index.html 200
