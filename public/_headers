# Cloudflare Pages 快取標頭。後面的規則會覆寫前面同名的標頭。
#
# 預設 no-cache（＝可以存，但每次都要回源驗證）。這條的對象是 HTML：
# index.html 內嵌的是**這次部署的資產檔名清單**，被瀏覽器快取住的話，
# 使用者「重新整理」拿回的還是舊清單，指向的 chunk 早已被新部署換掉，
# 於是怎麼重整都進不去——自癒路徑（見 src/utils/lazyWithRetry.ts）就死了。
/*
  Cache-Control: no-cache

# 資產檔名帶內容 hash，改動必換名，因此可以放心長快取。
/assets/*
  Cache-Control: public, max-age=31536000, immutable
