-- 會員驗證稽核紀錄的表註解：用語同步為「驗證」
--
-- 20260726000003 建表時註解寫的是「核身」。該版已套用到正式站，依慣例不回改
-- 已套用的 migration（改動已套用檔會讓檔案內容與資料庫實際執行過的東西對不上），
-- 所以另補這一支把註解帶到新用語。
--
-- 為什麼值得補：表註解會顯示在 Supabase Studio，是「核身」在資料庫這側唯一
-- 還看得到的地方；產品其餘各處（admin 頁、會員端、Edge Function 訊息、規格書
-- §13.1）已於用語統一那次一併改完。用語的單一事實來源是規格書 §13.1。
--
-- 只改註解：無 schema 變更、無資料異動。comment on 本身即冪等（直接覆寫既有值），
-- 重跑安全。

comment on table public.member_verify_logs is
  '會員身分驗證稽核：admin 掃碼驗證成功時逐次寫入（append-only）。查閱走 Supabase Studio，本期無前端介面。';
