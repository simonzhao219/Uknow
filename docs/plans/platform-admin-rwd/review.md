# 平台管理後台 RWD 規劃書審查報告

<!-- 由 /review-plan 彙整四個 reviewer subagent 的發現而成。
     聚合規則:只彙整、去重、排序,不改判。 -->

審查對象:`./plan.md`（規劃，非實作 diff）
審查者:`plan-reviewer-{system,architecture,uiux,requirements}`（四個 fresh-context subagent，平行派出）
＋**主 session 覆核**（見文末獨立節；與 subagent 發現分開記，不混入其判斷）

## 審查結論

| 視角 | P0 | P1 | P2 | 無缺口面向 |
|---|---|---|---|---|
| 系統 | 0 | 1 | 0 | API 契約、資料庫、外部整合、邊界條件、其餘資料流 |
| 架構 | 0 | 1 | 4 | appShell/lazy 契約、慣例相容性、階段依賴順序、JS 判定 vs CSS 的整體取捨 |
| UI/UX | 0 | 2 | 3 | §4.1 資訊量與危險操作、BottomNav 碰撞處理、畫面涵蓋完整性 |
| 需求 | 0 | 3 | 2 | 需求溯源（抽查 7 條全部準確）、業務規則、三態完備與可測試性 |
| 主 session 覆核 | 0 | 2 | 2 | 溢版巡檢報告的重複 path 處理（查證後不成立，見 N0） |
| **去重後合計** | **0** | **8** | **10** | |

**無 P0。** 沒有任何一個視角發現「照此規劃實作會做錯功能、壞既有行為、
或造成資料/資安事故」的缺口。八項 P1 全屬「規劃有實質缺口，實作前應補」。

⚠️ **但嚴重度分佈會誤導**：主 session 覆核另指出一項**跨越全案的方法論缺陷**
（驗證是套套邏輯，見文末「M1」），它本身不是單一 P 級發現，卻是本規劃
最該擋下開工的理由——單看「無 P0」會低估風險。

---

## 發現清單（依嚴重度排序）

### P1

**F1 [P1]〔§2 系統設計 / §4.1 匯款作業面板〕（系統）**
匯款作業面板顯示哪一筆，完全由 `activeId` 決定，而它**唯一的寫入點**是
`TableRow onClick={() => setActiveId(w.id)}`（`WithdrawalManagement.tsx:768`）。
§4.1 只描述手機卡片要顯示哪些*欄位*，沒說卡片是否接手這個職責——桌面表格
在手機被拿掉後，`activeId` 沒有任何寫入路徑，作業面板會永遠釘死
`withdrawals[0]`，展開也切不動。這正是「本次只改同一份資料怎麼排版」不成立
之處：排版變更悄悄拿掉了一個互動。
對照組：`MemberManagement` 的「查看」是顯式按鈕（`:111`），不依賴 row 結構，
轉卡片沒有這個問題——所以這不是表格轉卡片的通例陷阱，是本元件特有的隱性耦合。
→ 建議：在 §4.1 明確定義手機卡片是否要有等同的「設為目前作業對象」互動
（卡片本身或卡片內一顆按鈕觸發 `setActiveId`），或明確裁決「手機版作業面板
固定顯示第一筆、不提供切換」並寫進 §4.1。兩者皆可，但不能沒答案。

**F2 [P1]〔§4.1 頂層 Tabs / §4.0 P1〕（UI/UX）**
`grid-cols-3 md:grid-cols-5 + h-auto` 這組 class 清單**漏了無前綴的 `grid`**，
依字面實作很可能是 no-op。`TabsList` 原語預設 `inline-flex ... flex`
（`ui/tabs.tsx:32`），`grid-cols-*` 只是 `grid-template-columns`，對
`display:flex` 容器毫無作用。現況碼 `AdminDashboard.tsx:122` 的
`w-full md:grid md:grid-cols-5` 已自證這點——作者刻意在 `md:` 明寫 `grid`
才能覆寫 base 的 `flex`。若照字面做，手機容器仍是 flex（無 `flex-wrap`），
標籤維持單行 + `overflow-x-auto` 接手捲動，**P1 完全沒被修好、U2 驗收落空**。
→ 建議：§4.1 寫出完整 class（至少含無前綴 `grid`），階段 1 的驗證標準也要
能量出「兩列排列」而不只是「TabsTrigger 存在」。

**F3 [P1]〔§5 階段切分 / §7 風險表 / §8 可測試性〕（架構 + UI/UX，同一缺口）**
五個階段裡**沒有任何一項對純 CSS 版面修改做「真瀏覽器渲染且會擋 CI」的驗證**。
jsdom 不跑版面引擎，只能斷言 class 字串或 DOM 結構存在，量不出 P1（TabsList
兩列）、P5（Dialog 寬度）、P6（雙圖）、P9（Sheet 的 `dl`）是否真的照預期渲染。
唯一碰真瀏覽器的階段 5 是 report-only（`STRICT` 是套用到全部 `ROUTES` 的
單一全域開關，`SweepRoute` 無逐路由 override），而且該檔自己文件化
「對話框／下拉／Sheet 需要點擊才開啟」是已知盲區（`test_overflow_sweep.py:352`）
——P5/P6/P9 剛好全在 Dialog/Sheet 裡，階段 5 擴充後仍掃不到。
**上面的 F2 就是這個驗證缺口會放過的典型案例**：規劃內所有測試都會綠燈，
使用者看到的仍是壞的。
（同一缺口再一例：`TabsList` 若把 `h-9` 換成 `h-auto`，`TabsTrigger` 未同步
調整的 `h-[calc(100%-1px)]`（`tabs.tsx:53`）在 auto-height 容器下的百分比
高度解析，也只有真瀏覽器才確認得了。）
→ 建議：為涉及實際版面呈現的驗收標準（尤其 U2「五個分頁標籤同時可見」、
U3「照片寬度貼齊螢幕」）補一項會擋 CI 的量測（Playwright `getBoundingClientRect`
量 TabsList 是否分兩列、Dialog 寬度與視窗邊界的間距）；並評估讓 `SweepRoute`
支援逐路由 `STRICT` override，不必動全域開關。§7 該列風險的「緩解」文字也要
誠實反映現況。

**F4 [P1]〔§3 動到的模組 / §4.1 觸控〕（需求判 P1；UI/UX 判 P2）**
`ui/checkbox.tsx` 的 `pointer-coarse` 改動 blast radius 超出 §3 清單：該元件
還被 `WithdrawalProcess.tsx`、`JoinReferralProgramDialog.tsx`、`CompleteProfile.tsx`、
`CreateServiceProvider.tsx`、`EditServiceProvider.tsx` 這 **5 個會員端頁面**共用，
但它們不在 U1–U6 任何一條故事裡，§5 也沒有為它們排任何回歸驗證。§7 風險表
只給了「機制上安全」的理由（pointer-coarse 觸控專屬、滑鼠不變），沒給
「已驗證外觀不破版」的驗收方式。
（UI/UX 補充：這 5 頁多半是 `<Checkbox>` + `<Label htmlFor>`，原生 `label for`
已把可點區延伸到整段文字，把可見方框放大到 44×44px 對它們是次要影響；做法本身
沿用 `button.tsx` 既有先例，模式一致性站得住腳，純粹是風險面覆蓋不完整。）
→ 建議：在 §5 某階段（建議併入階段 1）為這 5 頁各加一則最小驗證，或至少在
§7 風險表補一行明確的驗收方式／明文豁免。

**F5 [P1]〔§3 動到的模組 / §5 階段切分〕（需求）**
`AdminSetup.tsx`（帳號資訊列長 Email 換行）被列進 §3 要動的檔案，但
§1 沒有對應故事、§4.0 沒有 P# 證據項、§5 五個階段的測試檔清單也都沒提到它
——**這個改動沒有進任何紅綠循環**。實作時要嘛被遺漏（§3 白紙黑字的承諾落空），
要嘛在階段外被無測試地順手改掉（違反「每階段 = 一個 TDD 紅綠循環」）。
→ 建議：補 §4.0 證據項與故事、在某階段（建議階段 1）加 `AdminSetup.test.tsx`；
若決定這次不處理，就從 §3 刪除，不要留一個沒有實作路徑的承諾。

**F6 [P1]〔§6 Q1〕（需求）**
Q1 引用 W8 理由只取了 `WithdrawalManagement.tsx:167`「標記已匯款需要同時開著
網銀」半句，**漏了緊接的 `:168`**「退件與代為完成不鎖——那是客服接到電話
當下就該能處理的事」。這句直接說明「pending 提領手機上只能退件」**不是意外的
不對稱缺陷，而是原作者已權衡過的設計**。Q1 用「不對稱的危險」措辭、在漏引
這句的情況下，容易讓裁決者誤以為發現了新疏漏，而非重新評估一個已有理由的
既有設計——Q1 涉及金流操作的權限邊界，裁決者需要完整脈絡。
→ 建議：在 Q1 補引 `:168` 的完整理由。
**（已於本次修訂處置，見下方「處置」節。）**

### P2

**F7 [P2]〔§4.0 P5〕（UI/UX）** — P5 的**診斷結論與修法方向正確**，但量化的
症狀描述站不住腳。`DialogContent` 同時帶 `w-full`（`dialog.tsx:41`）；對
`position:fixed` 元素，`width:100%` 已依視窗寬度定值（祖先鏈 `globals.css`、
`App.tsx` 的 `<main>` 均無 `transform`/`filter`/`will-change` 建立新包含區塊），
`max-width` 比目前寬度大時不生效。真正的後果是**失去 `calc(100%-2rem)` 的
左右 1rem 安全邊界、對話框貼齊螢幕邊緣**，不是產生 768px 寬度與水平捲動。
→ 若日後拿「是否出現水平捲軸」（`scrollWidth > innerWidth`）當驗證依據，
永遠測不出這個真正的退化。改量 dialog `getBoundingClientRect` 相對視窗邊界
的間距。

**F8 [P2]〔§3 偏離理由 1〕（架構）** — 「同一檔案裡一半靠 CSS 一半靠 JS 是
兩套真相」這條理由**只在 `WithdrawalManagement` 成立**（它已有 W8 的
`isDesktop`）；`MemberManagement` / `SystemAlerts` 目前完全沒有既有 JS media
邏輯，對這兩檔套用同一條理由，等於用另一個檔案的既存包袱正當化「無中生有」
引入 `useMediaQuery`。真正對三檔都站得住的是理由 2（DOM 成本）與理由 3
（可測試性）。→ 若 Q3 通過、§7 要補判準文字，判準不該照抄理由 1 的措辭，
只用理由 2/3 立論，避免未來規劃拿一個對其他元件不成立的先例套用到別處。

**F9 [P2]〔§3 / 共用原語〕（架構）** — 三個元件會各自呼叫
`useMediaQuery('(min-width: 768px)')`，測試側的 `stubMediaQuery`（~12 行，
`WithdrawalManagement.test.tsx:25`）也會在階段 2–4 的測試檔各自重貼一份——
這正是 `useMediaQuery.ts` 檔頭註解點名要避免的模式。→ 建議階段 1 順手抽出
共用的 `stubMediaQuery` test util（比照 `StatCardGrid` 的抽取先例）。
卡片 markup 本身**不建議**強行抽共用元件——三張卡的欄位/操作鍵差異夠大
（11 欄+5 Dialog vs 8 欄+3 鍵 vs 6 欄+JSON 收合），抽象成本可能高於重複成本。

**F10 [P2]〔§3 / 長期成本〕（架構）** — `WithdrawalManagement.tsx` 已是全 admin
最重的容器（849 行，狀態管理 + 5 個 Dialog + CSV 匯出全在一檔），階段 2 要再
疊一整套卡片渲染，半年後會逼近甚至超過 1000 行、身兼「業務邏輯 + 兩套完整 UI」。
規劃完全沒提是否拆分。→ 建議把手機卡片列表抽成同資料夾子元件
（如 `WithdrawalCardList.tsx`），用 props 接住 record 與既有 handler——與 §7
風險表「共用同一組 handler 與同一個 record 物件」方向一致，甚至更容易靠型別強制。
`MemberManagement`（474 行）風險較低但精神相同；`SystemAlerts`（147 行）可不拆。

**F11 [P2]〔§1「不做什麼」〕（需求）** — 第 3 項「不改 W8 的行動端權限邊界」被
放進**明確排除**清單本體，但同一句話又引用 Q1（未裁決的開放問題），與清單其他
三項（已定案的排除）性質不同，構成清單內部語意不一致——讀者可能誤把它當定案。
→ 改標注為「預設維持（見 Q1），非最終定案」，或只留在 §6。

**F12 [P2]〔§1 U4 / §5 階段 3〕（需求）** — U4 的「三顆操作鍵都在拇指可及處
**且 ≥44px**」是複合驗收標準，但階段 3 的驗證標準弱化成「三顆操作鍵可達」。
「拇指可及處」是版面位置的人因判準，jsdom 量不出 viewport 幾何。這是 jsdom 的
固有限制、不是規劃的錯，但規劃沒有承認這個落差，讓驗收標準看起來全部可自動驗證。
→ 階段 3 驗證標準明確切分「≥44px 與存在性由測試釘住；拇指熱區位置由人工目視驗收」。

**F13 [P2]〔§5 階段 2 驗證標準〕（架構）** — 「桌面：既有 40 條測試全綠不改」
數字有誤，`WithdrawalManagement.test.tsx` 實際是 **26 個** `it(...)`。
（三檔加總 477+332+166=975 行與規劃書宣稱的「975 行」完全吻合，顯示規劃確實
查過 codebase，只是這處條數寫錯。）不影響「全綠不改」這道真正的閘門。
→ 訂正數字，避免實作者對照時去找不存在的 14 條測試。
**（已於本次修訂處置，見下方「處置」節。）**

**F14 [P2]〔§3 動到的模組〕（UI/UX）** — `SystemAlerts.tsx` 的載入態是單一置中
`Loader2` spinner（`:84-86`），違反 `ui-ux-guidelines.md` §5「列表載入用骨架屏，
不要單一置中 spinner」。這是**既有缺口、非本次 RWD 引入**，不影響規劃正確性；
但該檔已列在 §3（階段 4），依「碰到的檔案順手還債」的童子軍原則，規劃書沒交代
要不要順手換成 `Skeleton`。→ 標為裁決項：順手修或明文不修，兩者皆可。

---

## 主 session 覆核（獨立於四個 subagent）

<!-- 聚合規則要求主 session「只彙整不改判」——那是針對 subagent 的發現。
     本節是主 session **自己驗證後新增**的發現，與上方分開記，避免混淆
     誰判了什麼。上方 F1–F14 的 severity 一律維持 reviewer 原判。 -->

主 session 逐條複驗了四個 subagent 的斷言（不照單全收），並自行對 codebase
取證。結果：**F1、F2、F3 全部成立**，另發現四項他們集體漏掉的缺口，以及
一項貫穿全案的方法論問題。

### N0 —— 查證後**不成立**，記錄以免日後重複懷疑

主 session 原本懷疑階段 5 讓 `/admin` 用同一個 `path` 掃五次，會在報告裡
互相覆蓋。查 `e2e/test_overflow_sweep.py` 的 `_write_report`：`results` 是
**list**、以 `label` + `path` 一起呈現、按發現數排序，重複 path 不會撞 key。
**這不是問題**，不列為發現。

### N1 [P1]〔§5 驗證標準〕覆蓋率棘輪會擋 CI，而規劃從頭到尾沒提

`npm run check`（＝ pre-commit 跑的那個）**不含覆蓋率**：
`biome check && tsc --noEmit && vitest run && knip`。覆蓋率門檻是 CI 另外跑
`npm run test:coverage` 把關（`.github/workflows/ci.yml:243`）。

門檻 `functions: 54`，而 2026-07-26 的實測值是 **55.21——只有 1.2 點餘裕**
（`vitest.config` 的棘輪註解自己寫了這個數字）。規劃要在 `AdminSetup.tsx`
與 `SystemNotifications.tsx` 這兩個**目前零測試檔**的元件上加新 JSX，而 §5
的驗證標準只寫「vitest 綠」。

**後果：本機 `npm run check` 全綠不等於 CI 綠。** 這條會在階段 4（或更早，
若 F5 的 `AdminSetup` 改動照現況無測試落點地做掉）撞牆。
→ 建議：§5 每個階段的驗證標準加上「`npm run test:coverage` 四項門檻不降」，
並把 `AdminSetup.tsx` / `SystemNotifications.tsx` 的測試檔視為**前置條件**
而非可選項。

### N2 [P1]〔§4.1〕規劃指定用裸 `<details>`，但 repo 已有對應原語

`src/components/ui/collapsible.tsx`（Radix Collapsible）**已存在**，且
`CreateServiceProvider.tsx` 與 `EditServiceProvider.tsx` 都在用；
全站 `<details>` 用量是 **0**。

規劃在兩處指定 `<details>`——匯款作業面板摺疊（§4.1）與告警 `context` JSON
收合（§4.1）——等於在既有原語可用的情況下另起爐灶。這正是
`ui-ux-guidelines.md` 前言「規劃要發明新模式，得先說明為什麼既有模式不適用」
要擋的事，而 §4.1 沒有說明。

附帶代價：`<details>` 的開合狀態**不受 React 控制**，測試斷言方式與 Radix
那套不同，會在 admin 底下長出第二種摺疊慣例。
→ 建議：改用 `Collapsible`，或在 §4.1 寫明為什麼 `Collapsible` 不適用。

### N3 [P2]〔§3 / §5 階段 1〕「標題區字級」是一個無證據的承諾

§3 的模組表與 §5 階段 1 都列了「標題區字級」，但 **§4.0 的 P1–P14 沒有對應
證據項、§4.1 也沒說要改成什麼**。而且「平台管理」四個字在 `text-3xl`（30px）
下約 120px，375px 下不會破版。

這與需求視角抓到的 **F5（`AdminSetup` 有承諾、無測試落點）是同型缺陷**，
需求視角只抓到一個。差別在於 F5 至少對應一個真問題，這條**可能連問題都
不存在**——是比漏做更糟的「承諾修一個沒壞的東西」。
→ 建議：補證據項，或從 §3 與階段 1 刪除。

### N4 [P2]〔§3 效能〕「手機端 DOM 節點數下降」大概率是錯的

卡片把每個欄位拆成 label + value 兩個節點，一張提領卡（8 個欄位 ＋ 多顆
操作鍵）的節點數**多於**一列 11 格的 `<tr>`。真正下降的是「相對於 CSS
雙套版面同時掛兩棵樹」，規劃把「相對現況」與「相對另一個方案」混為一談。

這正是規劃自己在 §6 要求的「每個斷言都要能指出依據」沒做到之處。
→ 建議：改寫為「相對於 CSS 雙套版面，手機端少掛一棵樹」，或刪除這條
未經量測的效能宣稱。

### 補 F2 —— reviewer 抓到一半，還漏了 `w-full`

UI/UX 視角正確指出 §4.1 的 class 清單漏了無前綴 `grid`。**但還漏了 `w-full`**：
`TabsList` 原語 base 是 `w-fit`（`ui/tabs.tsx:32`），現況碼靠
`w-full md:grid md:grid-cols-5` **三個一起**才成立。照 §4.1 字面實作，
即使補了 `grid`，容器仍會縮成內容寬度、三欄等分不會發生。
完整修法至少是 `w-full grid grid-cols-3 md:grid-cols-5 h-auto`。

---

## M1 —— 貫穿全案的方法論缺陷（**建議以此擋下開工**）

這不是單一 P 級發現，但主 session 認為它比任何一項 P1 都重要。

**這是一份 RWD 規劃，而它所有的自動化驗證都在 jsdom 裡斷言 class 字串，
但 jsdom 不跑版面引擎。**

具體到套套邏輯的程度：階段 1 的驗證標準是「checkbox 帶 `pointer-coarse`
class」——**這個測試斷言的是實作者剛打進去的那個字串**，它不可能為了正確的
理由失敗，也永遠抓不到真實觸控目標的回歸。同樣地，「五個 TabsTrigger 皆在
文件中」這條驗證，在 F2 描述的 no-op class 下**照樣全綠**。

把三項發現疊起來看，模式就清楚了：

- **F3**：五個階段都沒有「真瀏覽器且會擋 CI」的版面驗證
- **F2**：一個會讓修改完全失效的 class 錯誤，規劃內所有測試都抓不到
- **N1**：唯一會擋 CI 的另一道閘門（覆蓋率）規劃根本沒納入視野

唯一碰真瀏覽器的階段 5 是 report-only，且它自己文件化「對話框／下拉／Sheet
需要點擊才開啟」是盲區（`test_overflow_sweep.py:352`）——而規劃要修的
P5/P6/P9 **剛好全在 Dialog/Sheet 裡**。

附帶：`src/components/ui/` 目前**沒有任何測試檔**。用 `checkbox.test.tsx`
去斷言一個 class 字串，會替原語層開一個壞的先例。

### 建議的處置方向

不要照現在這份開工，也不要只是再補一輪細節修訂。**要動的是階段 0 的驗證
能力**：

1. 讓 `SweepRoute` 支援逐路由 `STRICT` override（不必動全域開關）
2. 讓巡檢能點開 Dialog/Sheet，消掉那塊已知盲區
3. 把 U2／U3 的驗收標準改成 Playwright 量 `getBoundingClientRect`
   （TabsList 是否分兩列、Dialog 與視窗邊界的間距、照片寬度）

有了會擋 CI 的真實版面量測，F2 那種 no-op 才會在階段 1 就紅——
**照現在的設計，它會一路綠到上線。**

---

## 需人工裁決

除規劃書 §6 已自行揭露的 **Q1 / Q2 / Q3** 外，reviewer 另提兩項：

- **Q4（需求提出）：「手機」的裝置定義邊界。** 規劃全篇以 375px 為驗證基準，
  但實際切版判準是 `useMediaQuery('(min-width: 768px)')`——**寬度，不是輸入方式**。
  W8 的鎖定用的是同一個判準，代表 768px 寬的觸控裝置（如 iPad）會被判為
  `isDesktop`、看得到「標記已匯款」，但那顆按鈕背後「同時開著網銀」的假設
  在單一觸控平板上未必成立。**這不是本次 RWD 引入的新問題**（W8 本來就用同一個
  判準），reviewer 明確表示不列為 P 級發現，但建議與 Q1 一併考慮：要不要用
  真正的觸控偵測取代寬度判準。

- **Q5（需求提出）：F4 的嚴重度。** 需求視角判 P1（「聲明了風險、但沒有對應
  驗收方式」的完整性缺口），UI/UX 視角判 P2。若人審認為 `pointer-coarse` 的
  機制保證已足夠、不需額外驗證那 5 個會員端頁面，可降級或豁免——**但降級理由
  應寫進下方「處置」欄**。
  <!-- 聚合規則:severity 以 reviewer 原判為準,本報告取較高者(P1)並保留兩造判斷,
       不代為改判。 -->

---

## 處置（人審後填寫）

<!-- P0 的處置規則:必須改 plan 並重跑 /review-plan,或由人在此明文豁免。
     tdd-implement 開工前會檢查:存在未處置 P0 → 拒絕開工。 -->

**無 P0，因此不觸發「修訂後重跑」的強制路徑。** 八項 P1 待人審裁決。

### ✅ M1 已處置（人審裁決：先補驗證能力）

人審採納「不要照這份開工，先補 M1 三點」。三點皆已完成並驗證，
**尚未動任何產品程式碼**：

| M1 | 做法 | 現況 |
|---|---|---|
| 逐路由 `STRICT` override | `SweepRoute.known_overflow`：非空才不擋 CI，**沒標的一律硬失敗**。取代原本全域開關的全有全無——只要還有一條髒的，已清乾淨的路由就一直沒有守衛 | 18/27 條上鎖，9 條已知債務逐條列出理由 |
| 巡檢點得開 Dialog/Sheet | `SweepRoute.post_nav`。Radix 的 `TabsContent` 不 `forceMount`，非作用中分頁**根本沒掛進 DOM**，admin 五個分頁有四個從未被量過 | 新增 admin 四分頁＋身分證對話框＋轉換歷史＋會員詳情 Sheet，路由 20 → 27 |
| U2/U3 改用真實幾何量測 | 新增 `e2e/layout_probe.py`（`count_rows` / `viewport_fit`）＋ `e2e/test_admin_mobile_layout.py`，以 `xfail(strict=True)` 表達終局 | 今天 xfail（CI 綠）；RWD 做完會 XPASS 而讓 CI 紅，逼實作者回來刪 marker |

`layout_probe` 與既有的 `overflow_probe` 刻意分家：**兩者的失效方式相反**。
溢出探針問「有沒有畫到框外」，對 `overflow-x: auto` 的容器刻意不報；
`layout_probe` 問「有沒有長成該有的樣子」。只靠前者，「沒壞但也沒對」的
版面（例如擠成單行捲動的 TabsList）永遠不會被發現。

`xfail(strict=True)` 的選擇是刻意的：它讓期望變成**會自我拆除的鷹架**，
而不是被註解掉的測試或沒有到期日的 TODO。此行為另以最小案例實測過
（修好的那個以 `[XPASS(strict)]` 失敗）。

**量測順帶裁決了三件先前只能靠推理的爭點**：

1. **F7 成立、規劃書 P5 的症狀描述不成立**（實測）。對話框在 375px 下
   **沒有溢出**——`w-full` 在 `fixed` 元素上已依視窗定寬 375px，`max-w-3xl`
   （768px）比它大所以不生效。真正的退化正是 F7 說的「安全邊距歸零、
   貼齊螢幕邊緣」，而那要量盒子才看得到。P5 的診斷（twMerge 蓋掉護欄）
   與修法仍然正確，錯的只有後果描述。
2. ~~**P10 描述的系統告警分頁實測 0 發現**，已改為上鎖。~~
   **⚠️ 這條後來被推翻——是假陰性。** 詳見下方「與 develop 合併後的訂正」。
3. **表格的橫向捲動不算溢出**：`Table` 原語的 `overflow-x-auto` 是明示要
   捲動，探針刻意不報。「要橫向捲才讀得完一列」（P2/P7）是可用性問題，
   不是溢出，這支測不出來——**不要因為那幾條綠了就以為 P2/P7 被守住了**。
   （原本這條寫成「P2/P7/P10 的寬表格永遠測不出來」，範圍過寬，
   見下方訂正。）

第 3 點對後續實作有直接影響：階段 2/3/4 的「表格→卡片」**沒有**機械把關，
仍然只能靠元件測試斷言結構＋人工目視。這個缺口是量測後才浮現的，
規劃書與四個 subagent 都沒有提到。

### ⚠️ 與 develop 合併後的訂正（推翻上面第 2 點）

合併 develop 時發現：**有人在 develop 上獨立實作了同一個想法**
（`SweepRoute.after_load` ≙ 我的 `post_nav`、切分頁 helper、tag 化的
pytest id），而且**帶了一份比我更兇的測資**——`_setup_admin_alerts`
取自正式站真實出現過的 `time_domain_backfill` 告警：長中文 message ＋
四欄 jsonb `context`。

用那份測資重量，系統告警分頁**溢出 294px**。

- **上面第 2 點是錯的。** 我先前量到「0 發現」是**假陰性**，成因是我的
  測資太弱（一句短訊息 ＋ 只有一個欄位的 context）。**規劃書 P10
  的描述其實是對的**，這條路由現在標了 `known_overflow`。
- **第 3 點原本的說法「寬表格永遠測不出來」範圍過寬。** 正確的界線是：
  表格在 `overflow-x-auto` 裡**橫向捲動**確實不算溢出，但**單一儲存格裡
  斷不了行的長字串照樣會報**——這裡就是 `whitespace-nowrap` 的 `td`
  裝著 JSON 原文。

這件事本身是對 `test_overflow_sweep.py` 檔頭那條「測資是最壞但可達」
原則的實證：**我的測資不是最壞的，所以量出了假的乾淨**。以後要把某條
路由標成上鎖之前，先確認測資真的取到產品允許的極端值。

**其餘未處置**：F1、F2、F4、F5、全部 P2，以及 N1–N4。它們涉及設計選擇或
範圍取捨，依「做事者不自評」原則留給人裁決。下一步仍是先處置這些，
再重跑 `/review-plan`，而不是直接 `/tdd-implement`。

### 已於本次修訂逕行處置的事實性訂正（非判斷題，人審仍可否決）

彙整過程中，`plan.md` 已就四處**純事實錯誤**當場修正——這些不是設計選擇，
是引用錯誤，留著會誤導後續實作：

| 對應發現 | 訂正內容 |
|---|---|
| F6 | §6 Q1 補引 `WithdrawalManagement.tsx:168` 的完整理由，並改寫措辭為「原作者權衡過的設計，不是意外的疏漏」 |
| F13 | §5 階段 2 的「既有 40 條測試」訂正為「26 條」 |
| F3（部分） | §7 風險表末列改為誠實陳述：階段 1 的 jsdom 測試只驗結構、量不出真實版面，是否溢出要到階段 5 的 Playwright 巡檢才看得到，而該巡檢預設 report-only |
| （需求視角附帶的 citation 瑕疵） | §4.0 P12 的行號 `SystemNotifications.tsx:236` 訂正為 `:243` |

**F1、F2、F4、F5、全部 P2，以及主 session 覆核的 N1–N4 未處置**
——它們涉及設計選擇或範圍取捨，依「做事者不自評」原則留給人裁決。
（**M1 已處置**，見上方一節。N4 與「補 F2」雖然也是事實性錯誤，但修正它們
會連帶改變 §4.1 的實作指示與 §3 的效能論述，不屬於單純的引用訂正，
故一併留給人審。）

- [ ] 人審完成，裁決:□ 通過 □ 修訂後通過（豁免理由：） □ 退回重規劃
