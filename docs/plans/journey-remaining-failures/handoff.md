# journey 剩餘失敗的交接文件

> **這份檔案的存在本身是今天的教訓。** 上一輪的交接文件 commit 在一條沒被
> fetch 的分支上,接手的 session 用 `git log --all` 搜不到就判定「不存在」,
> 從零重做了一整輪靜態分析(所幸結論一致)。**`--all` 只看本地 refs。**
> 這份直接進 develop,任何 session `git pull` 就看得到。

分支基準:develop @ `0858c19`(PR #265 合併後)
最新一場 journey:**run 31235468231 → 10 failed / 87 passed**

## 0. 已經修完的(不要重做)

| 根因 | 性質 | PR |
|---|---|---|
| 刊登名稱 17 字被 `#name` 的 `maxLength=10` 靜默截斷 | 測試 | #260 |
| 建立成功後沒清 `userListing` 快取 | **產品** | #260 |
| 提領查詢用了不存在的 `withdrawals.created_at` | 測試 | #265 |
| 兩個情境搶同一個人的當日提領額度 | 測試 | #265 |
| f45 `rls_probe.classify` 把 42501 判成 unauthenticated | 測試 | #257 |
| **S1** 推薦樹以未遮罩原名比對 UI(gen ≥ 2 會遮) | 測試 | 見下方 §S1 |

前兩條的完整紀錄見 `docs/plans/fix-journey-f40-listing/fix.md`(已結案,
兩個根因都經真後端複驗)。

## 1. 進度(2026-08-08 更新)

| 組 | 狀態 |
|---|---|
| **S1 推薦樹遮罩** | ✅ 已修(PR #269,另一個 session) |
| **S5 管理台退件退錯人** | ✅ 已修(PR #268) |
| **S3 失效會員看不到獎勵頁** | ✅ 已修(PR #268,**動到產品**+規格 §6) |
| **S4 fresh 付款沒過二次確認** | ✅ 已修(PR #268) |

**五組全部修完,不要重做。** 批次驗證已完成,結果見下。

### 批次驗證的場次紀錄

| run | 模式 | 結果 | 有效性 |
|---|---|---|---|
| 31235468231(`56aad2c`,基準) | sandbox | 10 failed / 87 passed / 0 skipped,20:19 | ✅ 基準 |
| 31253115394(develop @ `31388da`) | sandbox | 12 failed / 60 passed / **39 skipped**,3:54 | ❌ **無效** |
| 31263854444(同 SHA) | webhook | **8 failed / 103 passed / 0 skipped**,15:26 | ✅ 有效 |
| 31265631149(**同 SHA、同模式**,對照場) | webhook | **2 failed / 109 passed / 0 skipped**,18:03 | ✅ 有效 |

> collected 從 97 變 111 **不是情境變多**——兩個 commit 的 `features/*.feature`
> 各是 50 條、逐檔相同。多出來的 14 個是 S1 新增的 `tools/` 離線測試,
> 與 journey 情境同 rootdir 一起被收集。比對兩場時別把它讀成情境增減。

### 驗證結論:九條裡六條真的綠了,三條換了症狀

| 基準場的失敗情境 | 這場 | 判定 |
|---|---|---|
| f20 Root 推薦樹只顯示三代且第四代不出現 | ✅ 綠 | **S1 修法成立** |
| f60 上線的組織圖顯示已失效節點且結構不斷開 | ✅ 綠 | **S1 修法成立** |
| f60 過期會員的點數保留不歸零 | ✅ 綠 | **S3 修法成立** |
| f60 過期會員提領被擋(點數保留、僅擋提領) | ✅ 綠 | **S3 修法成立** |
| f60 新約復活(換推薦人、效期自付款日起算) | ✅ 綠 | **S4 修法成立**(webhook 模式仍走 A15 二次確認) |
| f50 完整生命週期:申請→匯款→查收 | ✅ 綠 | `requested_at` 欄位修法成立 |
| f50 退件路徑:點數退回 | ❌ **換症狀** | 見下 |
| f70 第 6 章 q9 | ❌ **換症狀** | 見下 |
| f70 第 7 章 s9 與 q14a | ❌ **換症狀** | 見下 |
| f70 第 10 章 終章對帳 | ❌ 同症狀 | 見下 |

**症狀變了代表修法生效、但露出下一層**——不是「沒修好」。最有價值的一條:

- `f50 退件路徑` 基準場停在**點不到退件鈕**(20 秒逾時);S5 把管理台退件改成
  以會員鎖定之後,退件真的執行了,於是測試往前走一步,卡在下一個斷言:
  **`退件後可提領 370P,未恢復為 1385P——點數未退回`(`assert 370 == 1385`)**。
  這是一條**新的、更深的**發現,而且碰的是點數,優先級最高。

### 這場新浮現的 4 條(基準場是綠的)

情境集兩邊逐條相同,所以這 4 條是**新紅**,不是新增情境:

| 情境 | 症狀 |
|---|---|
| f30 領取推薦王獎勵後訂閱到期日延長約一年 | 等不到 `身分證驗證成功`(10s) |
| f60 過期補繳:效期接續原到期日 | 等不到 `auth-login-button`(5s) |
| f70 第 5 章 b 樹下線 w2 掛 k0y1 | 等不到 `auth-signup-button`(5s) |
| f70 第 8 章 credit 與 a8 | `K0 沒有任何月桶,無法決定 credit 的 month_key` |

⚠️ **f70 是章節相依的 saga**:第 5 章倒了,6/7/8/10 的資料前提就不成立。
第 7 章的 `monthly_referrals 是空的`、第 8 章的 `K0 沒有任何月桶`、第 10 章的
`等不到「獎勵-子代續約」`都符合下游缺資料的形狀。**先確定第 5 章是不是單一
根因,再逐條歸因**——否則會把一個根因當成四個修。

`auth-login-button` 與 `auth-signup-button` 在兩支不同的步驟檔各失敗一次,
形狀相同(auth 入口頁沒渲染出按鈕),優先當**同一個根因**查。

---

## 2. 這批新紅的診斷(2026-08-08)

### 2a. auth 按鈕不出現 = check-email 限流(強假說,已下防線)

`POST /auth/check-email` 有 **per-IP 限流 10 次 / 5 分**
(`index.ts` → `bump_rate_limit`)。整套 journey 從 runner **同一個 IP**
出發,所以那 10 次是**全 session 共用**的。撞到之後前端只把 429 轉成一個
**會自己消失的 toast**(`AuthPage.tsx` 的 catch),畫面停在步驟 1
——測試看到的就是 `auth-login-button 不出現`,與「頁面沒載入」「帳號不存在」
長得一模一樣。

既有重置有三處(`steps/conftest.py` 的 per-scenario autouse、`org_builder`
的建樹波次、`f70` 的 `_ensure_actor`),但它們只保證**情境開頭**有配額。
一個情境內連續換身分登入好幾次是常態(f50 的完整生命週期是
會員 → 管理員 → 會員),**配額在情境中途見底沒有任何人管**。

這解釋了為什麼「沒改程式卻多了紅的」:webhook 模式跑得快(15:26 vs 20:19),
同樣的登入次數被壓進更少的 5 分鐘窗口,於是**越過門檻的是不同情境**。
形狀相同、名單會飄,正是共用配額被時序決定的樣子。

⚠️ **這是強假說,不是已證實的結論**:log 裡沒有 429 的直接證據(journey
的 log 不收網路)。所以修法刻意選了**會自己產出證據**的形狀——
`auth_gate` 只在畫面上真的出現限流訊息時才重置重試,其餘一律帶著
`dump_page` 的頁面文字失敗。下一場不論綠紅都會比這場多知道一件事。

### 2b. f50 退件退點:先斷言近因,再斷言衍生結果

`assert 370 == 1385` 看起來像「產品少退錢」,但那一步的前提(退件**有沒有
成功**)當時根本沒被斷言——feature 把 `狀態為 rejected` 排在點數之後。
已對調,並在點數斷言失敗時一併印出提領列與該筆的 `adjustment` 補償入帳:

- 沒有 adjustment → `admin_update_withdrawal_status` 沒插補償(產品)
- 有 adjustment 卻餘額沒回 → `reward_balances.available` 彙總(產品)
- 狀態根本不是 rejected → 退件動作靜默失敗(測試/UI)

三種修法完全不同,靠一句「點數未退回」分不出來。靜態追過一次:RPC →
adjustment → `available = sum(amount)` 的鏈是完整的,**所以不宜先當產品
bug 修**;拋棄式分支已刪,DB 證據取不回,只能讓下一場自己說。

### 2c. 沒動的:f70 第 7/8/10 章

第 5 章倒了它們就沒有資料前提。**先看第 5 章修好後還剩幾條**,不要現在
逐條歸因——那是把一個根因當四個修。

### 2d. 對照場(run 31265631149)——名單會飄,只有一條是決定性的

**同一顆 SHA、同一個模式、同一份程式碼**再跑一次:**8 failed → 2 failed**。

| 情境 | 31263854444 | 31265631149 |
|---|---|---|
| f30 領取推薦王獎勵 | ❌ | ✅ |
| f60 過期補繳 | ❌ | ✅ |
| f70 第 5 章 | ❌ | ✅ |
| f70 第 6/7/8/10 章 | ❌ ×4 | ✅ ×4 |
| f50 完整生命週期 | ✅ | ❌ |
| **f50 退件路徑** | ❌ | ❌ |

三件事一次確定:

1. **名單飄移 → 這批紅不是決定性的。** 支持 2a 的共用配額假說(越過門檻
   的是哪個情境由時序決定),也直接推翻「develop 上有回歸」這個讀法。
2. **f70 第 6/7/8/10 章確實是第 5 章的連鎖**——第 5 章一綠,四條全綠,
   不必逐條歸因(2c 的判斷成立)。
3. **只有 `f50 退件路徑` 兩場都紅**,而且兩場的死法不同:一場「點數未退回」,
   一場 **strict mode violation:同一個會員有兩列都可退件**。

第 3 點暴露的是**我自己在 PR #268 留下的 bug**:`_pending_row_of` 名字裡有
pending,實際上只用會員名過濾。同一個會員在提領管理上本來就會有好幾列
(申請過幾次就有幾列),前一個情境留下一筆 pending、這個情境再申請一筆,
就是兩列。

**同一個假設咬了兩次**:先是「清單第一列」,改成「該會員的列」之後又破——
兩次都是假設某個鍵唯一,而沒有任何東西保證它唯一。這正是 S1 學到卻沒有被
推廣的那一句(**識別鍵撐不住轉換就補一把結構性的鑰匙**)。已補的第二把
鑰匙是「該列真的提供這個動作」——產品只在 `status === 'pending'` 的列渲染
退件鈕,所以有這顆鈕就是這列是 pending 的結構等價物。鎖不到唯一一列時
**擲錯而不是取 `.first`**:取第一列正是要移除的假設,再套一次只會換地方重演。

同時補上**後置條件**:動作送出後等產品自己的完成回報(`ACTION_DONE` 的
`已退件：<會員名>`,那段字是刻意留在畫面上而不是 toast)。不等它的話,
後端擋掉這次轉換會**靜默**通過——run 31263854444 的「點數未退回」就有這個
可能,而當時完全看不出來。

兩者都由 `tools/test_admin_row_targeting.py` 在離線軌釘住。

第一場之所以無效:`f00_skeleton` 的 A0 付款在 PayUni sandbox 導頁後卡住
(`dump_page` 顯示欄位都正確填入、`確定鍵在送出前就可見:False`,停在
`sandbox-api.payuni.com.tw/api/upp`),於是 f10 建樹 `第 0 代有 1 個節點建置失敗`,
f20/f30/f40/f45/f50/f60 **全數 skip(38 條)**,f70 連鎖 10 條失敗。
**九條待驗情境有八條根本沒執行**——這場不是「還有 12 條紅」,是「什麼都沒驗到」。

已排除產品回歸:上一場成功的 run 之後 `src/**` 沒有任何改動碰到付款路徑
(`PaymentCheckout.tsx` 未動)。改走工作流內建的 `payment_mode: webhook`
(簽章注入備援)重跑;它仍會經過 S4 修的 A15 二次確認對話框
(`pay_fresh_via_gui` → `_arm_webhook_gateway` → `click_pay()` →
`fresh-confirm-dialog` → `_drive_payment`),所以 S4 的修法照樣被驗到。

⚠️ **`MIN_FULL=20` 的下限斷言擋不住這種場次**:整棵樹死了,但連鎖失敗仍
被計為「有執行」,所以下限過關、工作流不覺得異常。要判斷一場批次驗證有沒有
效力,看的是 **skipped 數**與**目標情境是否真的執行**,不是總數。

原訂順序 **S1 → S5 → S3 → S4**,一次一個 session、一個 PR 一個根因。
develop 高頻變動(今天一小時內合了 5 個 PR),**開工前先
`git fetch origin develop && git log --oneline HEAD..origin/develop`**
看別人在修什麼。

驗證策略:**批次**。各自 PR 進 develop 之後跑**一場** journey full 一次驗完
——牆鐘由 30 人 GUI 建樹主導,窄選省不掉,而分支費用每場都一樣。

⚠️ 窄選若真要用,**不能只帶 `listing` 這類 marker**:f40/f50/f60 的
Background 都需要 `@orgbuild` 的 30 人建樹,少了它情境會全 skip,
然後工作流的下限斷言以「實際只執行 0 個情境」硬失敗,整場白燒。

---

## S1 — 推薦樹展開(**已修,待下一場 journey 複驗**)

修法與根因如下,保留是因為 S3/S5 若碰到推薦網絡的任何斷言都適用同一組結論。
**動樹上的斷言一律走 `builders/referral_tree` 的 `expect_node` /
`expand_ancestors` / `tree_row`**,不要自己 `get_by_text(真名)`。

施工當下多發現一件規劃沒預料到的事,記在下方「額外發現」——照原規劃直接
把原名換成遮罩名會從逾時變成 strict mode violation,一樣是紅的。

**失敗情境**(2 條,同一個根因):

- `f20 Root 推薦樹只顯示三代且第四代不出現`
- `f60 上線的組織圖顯示已失效節點且結構不斷開`

**根因**:journey 用**未遮罩的原名**去比對 UI,但
`maskNameByGen()`(`supabase/functions/api/index.ts:3214`)對 **gen ≥ 2**
的姓名做隱私遮罩:

| 代 | 原名 | 畫面實際顯示 |
|---|---|---|
| gen 1 | `測壹捌零玖陸伍零乙參` | 原樣(不遮) |
| gen 2 | `測壹捌零玖陸伍零丙柒` | `測○○○○○○○○柒` |
| gen 3 | `測壹捌零玖陸伍零丁捌` | `測○○○○○○○○捌` |

規則:`gen <= 1` 或長度 ≤ 1 不遮;2 字 → 首字＋`○`;否則
首字＋`○`×(n-2)＋末字。非漢字用 `•`。

**為什麼失敗在那個位置**:`expand_ancestors(viewer=A0, target=D8)` 依
orgchart 走出鏈 `[C7, B3]`,反轉後先展開 **B3**(gen 1,**不遮**→ 成功),
再展開 **C7**(gen 2,**遮罩** → `treeitem` 的 `aria-label` 對不上 → 15 秒逾時)。
f60 等的是 D4(gen 3)—— 同一個根因。

**產品是對的**,遮罩是刻意的隱私設計(見 `/referrals/network/search`
端點註解:「回傳遮罩後顯示名…又不洩漏真名」)。**要改的是測試。**

### 額外發現:遮罩後的姓名**不再唯一**

journey 的姓名是 `測 + run_id 尾段 + 節點代號`(`tools/zh_names.py`),遮罩
只留首尾字,而尾字正是節點編號的數字——於是 **C7 與 D7 都遮成
`測○○○○○○○○柒`**。f20 展開到第三代之後,`測○…○捌` 同時命中 C8 與 D8,
只用姓名定位會撞上 Playwright 的 strict mode:症狀從逾時換成另一種紅。

定位鍵因此是**兩把鑰匙:遮罩後的顯示名 + `aria-level`**。orgchart 是嚴格
分層樹,某檢視者的第 k 代必定落在同一個絕對層,而**同一層之內遮罩後仍唯一**
——這個不變式由 `tools/test_name_mask.py` 的
`test_masked_names_stay_unique_within_each_generation` 鎖住,離線軌就會紅。

### 實際做的

1. `tools/name_mask.py` —— `maskNameByGen` 的鏡像;`tools/test_name_mask.py`
   直接讀 `index.ts` 源碼比對 `HAN_RANGE` 與遮罩規則的關鍵字面值,規則漂了
   在秒級的離線軌就紅(同 `test_listing_name.py` 讀 `NAME_MAX_LENGTH` 的做法)。
   順手把 `test_zh_names.py` 裡第三份 `HAN_RANGE` 複製品改成引用,Python 側只剩一份
2. `tools/orgchart.py` —— `ancestor_chain()` / `generation_of()`;target 不在
   viewer 下線鏈上時**擲錯**,先前會一路走到 root 交出不在該樹上的節點
3. `builders/referral_tree.py` —— `tree_row` / `expand_node` / `expand_ancestors`
   / `expect_node` 一律吃代數
4. `f20` 的 `tree_excludes(E1)` 是空洞斷言(它斷言 E1 的**原名**不出現,而遮罩
   之後原名本來就永遠不會出現,第四代真的漏出來時它也照樣綠),改成
   `推薦樹沒有第四代節點`:先確認第三代**已渲染**,再斷言沒有 `aria-level=4`
   的 treeitem,且第三代**沒有展開鈕**——把「此刻沒有」升級成「到不了」
5. `orgchart.yaml` 的 `expected_tree_visible` 沒有任何程式讀,其註解描述的正是
   被換掉的那條 E1 斷言,一併刪除

---

## S5 — f70 續約 saga ✅ 已修(PR #268)

**分類結果:測試脆弱,不是產品回歸。** `reject_first_withdrawal()` 退的是管理台
清單的**第一列**(全域,不限特定會員),退到別人的申請時 K0 那筆仍是 pending,
`hasPendingWithdrawal` 照樣為真 → 新約選項不解封。第 7、10 章是下游連鎖。

排除了快取假說:`login_via_gui` 會清 sessionStorage,而 DataCache 就存在那裡。
修法:三處位置選取全改成以會員鎖定,舊的位置版刪除。**舊註解自己就預言了
這次失敗並指定了修法。**

<details><summary>原始偵查紀錄(保留)</summary>

**失敗情境**(3 條):

- 第 6 章 `q9 防線:待審提領擋 fresh、駁回退點後解封` — `Locator expected to have count '0'`
- 第 7 章 `s9 與 q14a:填現任上代碼照樣清空歷史桶` — `Locator.click timeout 20000ms`
- 第 10 章 `終章對帳:分類軸、免費續約註記與推導餘額` — `Locator expected to be visible`

**為什麼優先**:這 3 條在 run 31234221750(02:06)**沒有**、在
run 31235468231(02:40)**有**。兩場之間的差異是 rebase 到含 #257/#261 的
develop。「上一場沒有、這場有」本身就是訊號,拖越久越難歸因。

**第一步是分類,不是修**:

- saga 是**章節相依**的,第 6 章壞了會拖垮 7、10 → 很可能**一個根因三條**
- 第 6 章是**提領**相關,而今天剛確認「每人每天只能提領一次、且不看狀態」
  (`has_withdrawn_today`)。**懷疑同類,但沒有證據前不要當結論**
- 也可能只是 saga 對執行順序敏感(非真回歸)

先回答「是真回歸還是順序敏感」再決定要不要走完整 `/fix-bug`。

</details>

---

## S3 — 過期會員與獎勵頁 ✅ 已修(PR #268,**動到產品**)

**關鍵:不能只是拿掉 guard。** `RequireMembershipRoute` 同時擋停權帳號與未完成
註冊的漏斗,整個拿掉會讓**停權者也看得到獎勵頁**。改成只放寬決策表第 4 格
(曾訂閱、已到期),並加 `expired-renewal-banner` 常駐續約提示。
規格 §6 同 PR 修訂——它原本與 §5 狀態表互相矛盾。

<details><summary>原始分析與裁決紀錄(保留)</summary>

**失敗情境**(2 條):`f60 過期會員的點數保留不歸零`、
`f60 過期會員提領被擋(點數保留、僅擋提領)`。
兩條都停在 `/payment/checkout`,等不到 `/rewards` 的「獎勵回饋」heading。

**衝突點**:

- **程式碼**:`/rewards` 包在 `RequireMembershipRoute` 裡(`App.tsx:375`)
  → 過期會員被導去續約頁
- **規格 §59**:「會籍失效…會籍限定功能被導向續約」→ 支持現行程式碼
- **規格 §5 狀態表**:失效時獎勵收益「✅ 保留不歸零」、提領「❌ 不可」
- **情境名稱**:「**僅擋提領**」→ 預期看得到頁面

張力在於:「點數保留不歸零」若使用者根本看不到,這個承諾等於不存在。

**使用者已裁決:選項 C** ——
**獎勵頁改為過期會員可看,但頂部常駐續約提示;提領動作維持擋。**

施工時記得:這會動到產品程式碼,**規格 §59 要在同一個 PR 回頭修**
(CLAUDE.md:規格與程式碼衝突時以程式碼為準,並在同一個 PR 修規格書)。
另外續約動線少了一個觸點,續約提示的顯著程度值得用 UI/UX 視角審一次。

</details>

---

## S4 — PayUni sandbox 導頁逾時 ✅ 已修(PR #268)

**先前「外部相依」的初判是錯的。** 根因是測試:fresh 模式點「前往付款」開的是
A15 二次確認對話框、不直接送出,而這條情境呼叫的是一般的 `pay_via_gui`,
停在對話框上永遠等不到跳轉。f70 同款情境前置流程一字不差卻用對了
`pay_fresh_via_gui`——兩處相同流程分岔。

漏看的訊號:2/2 穩定重現本來就該提示是決定性的;而 f10 建樹的 30 筆 sandbox
付款全部成功,早就說明 sandbox 本身是通的。

<details><summary>原始判斷紀錄(保留)</summary>

**失敗情境**:`f60 新約復活 — 換推薦人、效期自付款日起算、刊登重新公開`
—— 等不到導向 `sandbox-api.payuni.com.tw`,60 秒逾時。

**兩場 run 都失敗(2/2)**,所以**不是偶發**,先前「可能只是環境雜訊」的
判斷已被推翻。但它是外部相依,最可能白費工,所以排最後。

</details>

---

## 貫穿這批 bug 的一條原則

今天四個根因裡有三個是同一個形狀,**S1 是第四個**:

| bug | 產品實際做的轉換 | 測試以為的值 |
|---|---|---|
| f40 刊登名稱 | `maxLength` 截斷成 10 字 | 原始 17 字 |
| f50 提領查詢 | 欄位叫 `requested_at` | `created_at` |
| **S1 推薦樹** | **gen≥2 姓名遮罩** | **原名** |

**測試相信了一個產品從未承諾的值**,而且三次的錯誤訊息都指向遠處
(某個 `get_by_text` / `expect` 找不到東西),把人帶離現場。

可操作的收斂:**斷言一個值之前,先確認產品在那條路徑上不會轉換它**
——長度上限、遮罩、正規化 trigger、欄位改名都算。已補的
`BasePage.fill_exact()` 是這條原則的第一個機械化實例(填完回頭比對),
其餘目前仍靠人。

S1 施工時長出第二句:**轉換之後還要確認轉換後的值仍然唯一。** 遮罩、截斷、
正規化都會壓縮資訊,原本能認人的值壓縮完可能認到兩個人——那時失敗訊息會從
「找不到」變成「找到太多」(Playwright strict mode),一樣離現場很遠。
定位一個東西時,識別鍵撐不住轉換就補一把結構性的鑰匙(S1 補的是 `aria-level`)。

### 第二種形狀:兩份做同一件事的東西,默默分岔

| bug | 分岔在哪 |
|---|---|
| f40/f60 刊登名稱 | 兩支步驟檔各有一份逐字相同的名稱產生器 |
| **S4** fresh 付款 | f70 用 `pay_fresh_via_gui`(對),f60 用 `pay_via_gui`(錯),前置流程一字不差 |
| **S5** 管理台退件 | 三處都用「清單第一列」,依賴一個沒人保證的全域不變式 |

收斂:**同一件事只留一個實作**。三次的修法都是「收斂成一處」而不是
「兩處各修各的」——後者會再分岔一次。S5 那次更直接:舊註解自己就寫了
「若未來有 feature 留下 pending,改成 filter-by-member」,那個預言成真了,
而修法早就寫在註解裡沒人做。**預見到的風險要當場修掉,不是寫進註解等它發生。**

### 一次判斷失誤,值得記著

S4 一度被我初判為「外部相依、可能是環境雜訊,排最後」。錯了,根因在測試。
漏看的訊號有兩個:**2/2 穩定重現**本來就該提示它是決定性而非偶發;而 f10
建樹的 **30 筆 sandbox 付款全部成功**,早就說明 sandbox 本身是通的。
「外部相依」這個標籤壓過了手上的證據——**標籤不是證據**。
